#Conexion al config-service Spring Cloud Server
spring:
  application:
    name: microservice-uaa
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t
  profiles:
   active:
   - prod